class Task {
    constructor(taskId, title, description, statusId) {
        this.taskId = taskId;
        this.title = title;
        this.description = description;
        this.statusId = statusId;
    }
}

export default Task;